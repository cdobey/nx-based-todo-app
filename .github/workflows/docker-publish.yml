name: Build and Push Docker Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE_NAME: ${{ github.repository }}/backend
  FRONTEND_IMAGE_NAME: ${{ github.repository }}/frontend
  GRAPHQL_BFF_IMAGE_NAME: ${{ github.repository }}/graphql-bff
  HELLO_WORLD_IMAGE_NAME: ${{ github.repository }}/hello-world

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check if backend is affected
        id: affected
        run: |
          npx nx show projects --affected --base=origin/main~1 --head=HEAD > affected.txt
          if grep -q "backend" affected.txt; then
            echo "backend=true" >> $GITHUB_OUTPUT
          else
            echo "backend=false" >> $GITHUB_OUTPUT
          fi
          if grep -q "frontend" affected.txt; then
            echo "frontend=true" >> $GITHUB_OUTPUT
          else
            echo "frontend=false" >> $GITHUB_OUTPUT
          fi
          if grep -q "graphql-bff" affected.txt; then
            echo "graphql-bff=true" >> $GITHUB_OUTPUT
          else
            echo "graphql-bff=false" >> $GITHUB_OUTPUT
          fi

      - name: Skip message
        if: steps.affected.outputs.backend == 'false' && steps.affected.outputs.frontend == 'false' && steps.affected.outputs.graphql-bff == 'false'
        run: echo "No affected projects requiring Docker builds"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for backend
        if: steps.affected.outputs.backend == 'true'
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push backend image
        if: steps.affected.outputs.backend == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./packages/backend
          file: ./packages/backend/Dockerfile
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}

      - name: Extract metadata for frontend
        if: steps.affected.outputs.frontend == 'true'
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push frontend image
        if: steps.affected.outputs.frontend == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./packages/frontend/Dockerfile
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}

      - name: Extract metadata for graphql-bff
        if: steps.affected.outputs.graphql-bff == 'true'
        id: meta-graphql-bff
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.GRAPHQL_BFF_IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push graphql-bff image
        if: steps.affected.outputs.graphql-bff == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./packages/graphql-bff/Dockerfile
          push: true
          tags: ${{ steps.meta-graphql-bff.outputs.tags }}
          labels: ${{ steps.meta-graphql-bff.outputs.labels }}

      - name: Extract metadata for hello-world
        id: meta-hello
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.HELLO_WORLD_IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push hello-world image
        uses: docker/build-push-action@v5
        with:
          context: ./packages/hello-world
          file: ./packages/hello-world/Dockerfile
          push: true
          tags: ${{ steps.meta-hello.outputs.tags }}
          labels: ${{ steps.meta-hello.outputs.labels }}
